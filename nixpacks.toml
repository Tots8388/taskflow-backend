[phases.setup]
nixPkgs = ["python311", "pkg-config", "mysqlclient"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[phases.build]
cmds = ["python manage.py collectstatic --noinput"]

[start]
cmd = "gunicorn backend.wsgi --log-file -"